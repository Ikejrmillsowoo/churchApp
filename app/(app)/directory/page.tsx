// app/(app)/directory/page.tsx — member directory placeholder (built in Phase 4).
import { PlaceholderScreen } from "@/components/placeholder-screen";

export default function DirectoryPage() {
  return (
    <PlaceholderScreen
      title="Directory"
      description="Browse approved members of the church. Members choose whether to share their contact details."
    />
  );
}
