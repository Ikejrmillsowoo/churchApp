// app/(app)/calendar/page.tsx — events calendar placeholder (built in Phase 5).
import { PlaceholderScreen } from "@/components/placeholder-screen";

export default function CalendarPage() {
  return (
    <PlaceholderScreen
      title="Calendar"
      description="See upcoming church events and add them to your own calendar."
    />
  );
}
